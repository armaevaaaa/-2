# ================================================
# ИГРА «ПОДЗЕМЕЛЬЕ: СОЛЯНЫЕ ШАХТЫ»
# Автор: Армаева София
# Дата: сентябрь 2026
#
# Пункт 4 — «постучать по стене»: герой прислушивается
# к эху, чтобы понять, есть ли рядом пустоты. Пока
# пункт только выводится в меню — обрабатывать
# его будем на третьем занятии.
#
# Этап 2: добавлены характеристики героя (здоровье,
# сила, ловкость, выносливость), расчёт урона и
# формуляр. Четвёртая характеристика — выносливость.
# Запас сил = (здоровье + выносливость * 3) // 10.
# ================================================

# --- Заголовок ------------------------------------
title = "СОЛЯНЫЕ ШАХТЫ"
frame = "=" * 19
print(frame)
print(f"   {title}   ")
print(frame)
print()

# --- Знакомство с героем --------------------------
print("Как зовут героя?")
hero_name = input()
print(f"Добро пожаловать, {hero_name}!")
print("Ты входишь в соляные шахты. Стены покрыты кристаллами, а воздух сухой и колючий.")
print()

# --- Настройка героя -----------------------------
print("Настройка героя.")
print("Здоровье, сила, ловкость, выносливость – по одному числу в строке:")
health = int(input())
strength = int(input())
agility = int(input())
endurance = int(input())

# --- Расчёт урона --------------------------------
base_attack = 10
damage = base_attack + strength * 1.5
crit_damage = damage * 2
stamina = (health + endurance * 3) // 10

# --- Формуляр героя ------------------------------
print("Характеристики героя:")
print(f"Здоровье:   {health}")
print(f"Сила:       {strength}")
print(f"Ловкость:   {agility}")
print(f"Выносливость: {endurance}")
print()
print(f"Урон героя: {damage:.1f}")
print(f"Критический урон: {crit_damage:.1f}")
print(f"Запас сил: {stamina}")
print()

# --- Меню действий --------------------------------
print("Что делаешь?")
print("1 - осмотреться")
print("2 - идти вперёд")
print("3 - отдохнуть")
print("4 - постучать по стене")
print("5 - попробовать соль на вкус")
print()

# --- Выбор героя ----------------------------------
choice = input()  # ответ пока не используем — разберём на 3 занятии

# --- Прощание -------------------------------------
print()
print(frame)
print(f"Удачи, {hero_name}!")
print(frame)
